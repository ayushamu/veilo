# Security Notes

- Dependency advisory: `npm audit` currently reports a moderate PostCSS advisory through the installed Next.js release. Do not run `npm audit fix --force`, because npm proposes a breaking downgrade. Upgrade Next when a patched compatible release is available.

